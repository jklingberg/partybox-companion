# partybox

Python SDK for controlling PartyBox speakers over Bluetooth.

Zero runtime dependencies — uses Python's built-in `socket` module for Bluetooth RFCOMM.

Part of the [partybox-companion](https://github.com/jklingberg/partybox-companion) workspace.
